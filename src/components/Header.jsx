import * as React from 'react';
import {Heading} from 'grommet'

class Header extends React.Component {
  render() {
    return(
      <Heading style={{maxWidth: 1500}}>
        Only Gratitude
      </Heading>
    )
  }
}

export default Header